import React from "react";

export function App() {
  return (
    <div style={{ background: "#0f172a", color: "#f8fafc", minHeight: "100vh", padding: 24, fontFamily: "sans-serif" }}>
      <h1>AETHER Desktop GUI (Tauri v2 + React 19)</h1>
      <p>Node-graph workflow canvas and live trace inspector shell initialized.</p>
    </div>
  );
}

export default App;
